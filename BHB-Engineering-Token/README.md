# BHB Engineering Token (BHB)

[![BHB Token CI](https://github.com/y0bahrambeigi/bhb/actions/workflows/bhb-engineering-token-ci.yml/badge.svg)](https://github.com/y0bahrambeigi/bhb/actions/workflows/bhb-engineering-token-ci.yml)

بسته اجرایی یک توکن استاندارد ERC-20 و داشبورد مدیریتی برای شبکه آزمایشی Ethereum Sepolia، طراحی و توسعه‌یافته برای کاربردهای آموزشی، پژوهشی و مهندسی.

**طراح و توسعه‌دهنده:** یوسف بهرام بیگی (Yousef Bahram Beigi)

> این پروژه یک توکن روی اتریوم است، نه یک بلاک‌چین مستقل. BHB در نسخه حاضر فقط برای Sepolia طراحی شده و ارزش پولی واقعی ندارد.

## مشخصات توکن

| ویژگی | مقدار |
|---|---:|
| نام | BHB Engineering Token |
| نماد | BHB |
| استاندارد | ERC-20 |
| اعشار | 18 |
| عرضه اولیه | 100,000,000 BHB |
| امکان صدور بعدی | حداکثر 20,000,000 BHB |
| سقف قطعی | 120,000,000 BHB |
| شبکه هدف | Ethereum Sepolia |
| توقف اضطراری | دارد؛ فقط مالک |
| انتقال مالکیت | دومرحله‌ای |
| داشبورد وب | واکنش‌گرا، فارسی و قابل نصب |

صدور بعدی به‌صورت **تجمعی** محدود است. سوزاندن توکن، سهمیه صدور را دوباره باز نمی‌کند. بنابراین مالک در کل عمر قرارداد بیش از ۲۰ میلیون BHB اضافی صادر نخواهد کرد.

## طراحی امنیتی

- استفاده از OpenZeppelin Contracts 5.6.1؛
- سقف غیرقابل‌تغییر در کد قرارداد؛
- توقف اضطراری انتقال، صدور و سوزاندن؛
- صدور فقط توسط مالک و فقط در محدوده سهمیه باقی‌مانده؛
- انتقال مالکیت دومرحله‌ای برای کاهش خطر انتقال اشتباه؛
- جلوگیری از صرف‌نظرکردن مالک از مالکیت در حالت توقف، برای پیشگیری از قفل دائمی؛
- بدون مالیات انتقال، فهرست سیاه، در پشتی، ارتقاپذیری یا امکان افزایش سقف.

## اجرای محلی

پیش‌نیاز: Node.js نسخه 22.13 یا جدیدتر.

```bash
npm ci
npm run build
npm test
```

برای مشاهده داشبورد:

```bash
npm run dashboard
```

سپس `http://127.0.0.1:4173` را در مرورگر باز کنید. داشبورد بدون دریافت کلید خصوصی، از طریق کیف پول کاربر تراکنش‌ها را برای امضا ارسال می‌کند.

## امکانات داشبورد

- اتصال MetaMask یا کیف پول سازگار با EIP-1193؛
- اجبار اتصال به شبکه آزمایشی Sepolia؛
- اعتبارسنجی آدرس و هویت قرارداد BHB؛
- نمایش موجودی، عرضه فعلی، سقف قطعی و سهمیه صدور باقی‌مانده؛
- انتقال و سوزاندن BHB توسط دارنده؛
- صدور محدود، توقف اضطراری و انتقال مالکیت برای مالک؛
- پذیرش دومرحله‌ای مالکیت توسط مالک جدید؛
- پیوند مستقیم هر تراکنش به Sepolia Etherscan؛
- رابط واکنش‌گرا و PWA برای رایانه و تلفن همراه.

## انتشار امن روی Sepolia

### ۱. کیف پول آزمایشی

یک کیف پول جداگانه فقط برای Sepolia بسازید و مقداری Sepolia ETH از یک faucet معتبر دریافت کنید. هرگز عبارت بازیابی یا کلید خصوصی را در چت، GitHub یا فایل `.env` قرار ندهید.

### ۲. ذخیره رمزگذاری‌شده تنظیمات

```bash
npx hardhat keystore set SEPOLIA_RPC_URL
npx hardhat keystore set SEPOLIA_PRIVATE_KEY
```

برای تأیید کد در Etherscan:

```bash
npx hardhat keystore set ETHERSCAN_API_KEY
```

### ۳. انتشار و تأیید

```bash
npm run deploy:sepolia
npm run verify:sepolia
npm run sync:sepolia
```

هر دو فرمان Sepolia ابتدا فایل محلی `ignition/parameters/sepolia.json` را می‌خوانند و روی زنجیره کنترل می‌کنند که `initialOwner` یک Safe واقعی با پنج مالک مستقل و حدنصاب سه امضا باشد. ماژول اصلی هیچ مالک پیش‌فرضی ندارد و بدون پارامتر صریح Safe متوقف می‌شود. Hardhat Ignition آدرس قرارداد را پس از انتشار نمایش می‌دهد و فرمان `sync:sepolia` همان آدرس را از نتیجه رسمی انتشار استخراج و در داشبورد ثبت می‌کند.

اسناد اصلی پروژه:

- [لایت‌پیپر فارسی](docs/LITEPAPER_FA.md)؛
- [سیاست تخصیص و صدور](docs/TOKEN_ALLOCATION_FA.md)؛
- [سیاست مالکیت چندامضایی](docs/MULTISIG_GOVERNANCE_FA.md)؛
- [یادداشت دامنه حقوقی EU/آلمان](docs/LEGAL_SCOPING_EU_DE_FA.md)؛
- [درخواست ممیزی مستقل](docs/INDEPENDENT_AUDIT_RFP_FA.md)؛
- [بازبینی امنیتی خودکار](SECURITY_REVIEW.md)؛
- [فرم تأیید استقرار Sepolia](docs/DEPLOYMENT_SIGNOFF_FA.md)؛
- [راهنمای انتشار](docs/DEPLOYMENT_FA.md)، [بهره‌برداری](docs/OPERATIONS_FA.md)، [شفافیت](docs/TRANSPARENCY_FA.md)، [نقشه راه](docs/ROADMAP_FA.md) و [اعتبارسنجی](VALIDATION.md).

## عملیات مدیریتی مهم

مقادیر توکن در قرارداد ۱۸ رقم اعشار دارند. برای نمونه، صدور ۵۰۰ BHB برابر `500 × 10^18` واحد پایه است.

- `mint(recipient, amount)`: صدور محدود توسط مالک؛
- `pause()`: توقف انتقال، صدور و سوزاندن؛
- `unpause()`: بازگشایی عملیات؛
- `transferOwnership(newOwner)`: شروع انتقال مالکیت؛
- `acceptOwnership()`: پذیرش مالکیت توسط مالک جدید؛
- `remainingMintAllowance()`: سهمیه باقی‌مانده صدور؛
- `burn(amount)`: سوزاندن توکن توسط دارنده.

## آزمون‌های موجود

مجموعه ۱۷ آزمون، منطق قرارداد و سیاست preflight را پوشش می‌دهد: هویت و عرضه، تخصیص کامل خزانه به مالک تعیین‌شده، آدرس صفر، انتقال ERC-20، کنترل دسترسی، سقف تجمعی، invariantهای mint/burn، atomicity تراکنش ناموفق، توقف اضطراری، انتقال دومرحله‌ای مالکیت، جلوگیری از قفل دائمی و کنترل chain/Safe پنج‌مالکی با حدنصاب سه. آزمون مستقل پیکربندی استقرار نیز ثابت می‌کند ماژول اصلی بدون `initialOwner` شکست می‌خورد و مسیر محلی جداگانه همچنان کار می‌کند. گردش اجرایی CI در [`../.github/workflows/bhb-engineering-token-ci.yml`](../.github/workflows/bhb-engineering-token-ci.yml) قرار دارد.

## ساختار پروژه

```text
contracts/               قرارداد Solidity
test/                    آزمون‌های خودکار
ignition/modules/        ماژول انتشار قابل بازیابی
dashboard/               داشبورد وب و PWA
tools/                   سرور محلی و همگام‌سازی آدرس
docs/                    راهنمای انتشار و بهره‌برداری
```

## هشدار

قبل از هر استفاده عمومی یا مالی، ممیزی مستقل قرارداد هوشمند، ارزیابی حقوقی، سیاست شفاف تخصیص توکن و سازوکار امن نگهداری کلیدها ضروری است. این پروژه وعده سود، سرمایه‌گذاری یا ارزش بازار ارائه نمی‌کند.

## مشارکت

راهنمای اجرای تغییرات، استاندارد Pull Request و شیوه گزارش امن آسیب‌پذیری در [CONTRIBUTING.md](CONTRIBUTING.md) و [SECURITY.md](SECURITY.md) آمده است.

## License

MIT
